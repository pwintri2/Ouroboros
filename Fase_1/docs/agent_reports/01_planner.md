# 01 Planner

## Scope

Fase 1 only: build the vibrational consciousness core. Fase 2 is explicitly out
of scope and no Fase 2 files, agents, or behavior are planned.

## Inputs

- Workspace: `/tmp/codex-workspace`
- Reference-only project path found: `/home/pwintri2/WintripAI`
- Missing typo path: `/home/pwintri2/WintriAI`
- Seed knowledge source copied from:
  `/home/pwintri2/Documents/Wintrip AI aministratie/AGI Kennis.txt`

## Plan

1. Implement a living `HertzOscillator` with 418-432 Hz sinus motion and
   600-1200 Hz creative spikes.
2. Map current Hertz into temperature, curiosity, dwell time, scroll distance,
   and link-jump probability.
3. Implement a real Playwright browser engine with navigate, click, scroll,
   type, visible text reading, screenshots, and screenshot-to-vision handoff.
4. Implement ChromaDB hippocampus records with exactly the requested 11D schema
   and extra `current_hz` plus `vibration_mood`.
5. Implement minimal PAEU: Perceive, Act, Evaluate, Update.
6. Load `AGI Kennis.txt` and use its exact topics as the browser-learning queue.
7. Package everything for Docker Compose with Python, Playwright, ChromaDB, and
   Gradio dashboard.
8. Add local non-Docker tests and prepare Docker test/run commands, but do not
   execute Docker without explicit permission.

## Safety Boundaries

- No host OS modification.
- No destructive commands.
- Reference repository is read-only for this workflow.
- No Docker run, Docker Compose up, or deployment without explicit permission.
