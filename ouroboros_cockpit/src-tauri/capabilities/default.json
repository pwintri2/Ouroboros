{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Core permissions for the main Ouroboros cockpit window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
